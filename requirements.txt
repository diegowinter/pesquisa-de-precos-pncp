# Pipeline itens-contratos-atas v2
#
# OBSOLETO desde a Fase 0: a lista canônica de dependências passou a ser o `[project]` do
# pyproject.toml (`pip install -e .` / `uv run`). Mantido só para quem tinha o comando
# `pip install -r requirements.txt` na memória muscular. Editar os dois é como se perde
# a sincronia — edite o pyproject.
pandas
pyarrow
requests
python-dotenv
rich
openpyxl
langchain-openai
langchain-core
unidecode

# Etapa 5 (PDF/OCR)
pymupdf

# Etapas 6a/6b (GPU — rodam sozinhos na VRAM)
rank-bm25
sentence-transformers
